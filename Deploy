node {
git "https://github.com/talukas/WS"
def mvnHome = tool 'Maven_3.5.0'
sh "'${mvnHome}/bin/mvn' clean site -Dgroups=" + group
stage(Build & Deploy) {sh "'${mvnHome}/bin/mvn' clean package tomcat7"}
}
